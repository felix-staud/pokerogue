/**
 * Defines the speed of hp-bar animations
 */
export enum HpBarSpeed {
  /** The normal/default speed. */
  DEFAULT,
  /** Fast speed. */
  FAST,
  /** Faster speed. */
  FASTER,
  /** Skip animation. */
  SKIP,
}
